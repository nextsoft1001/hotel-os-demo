# Hotel OS — live demo

Static demo served at https://booking.clientneedz.com

- `index.html` — the hotel operating-system demo
- `tour.html` — the self-running guided tour

This repository is public only because GitHub Pages requires it on the free plan.
It contains no credentials, no database migrations and no server code — only the
two self-contained HTML files above.
